# 四人语言涌现实验 - 快速开始

## 🎯 目标

让四个AI角色（伊莎贝拉、玛丽亚、卡门、塔玛拉）使用Novlang符号语言进行高效持续对话，观察语言涌现现象。

## 🚀 快速开始

### 1. 激活环境

```powershell
conda activate generative_agents_cn
cd generative_agents
```

### 2. 启动实验

**方式一：使用快捷脚本（推荐）**

```powershell
# 开始新实验
.\run_experiment.ps1 -Action start -Name "test-1" -Rounds 100

# 继续实验
.\run_experiment.ps1 -Action resume -Name "test-1" -Rounds 50

# 分析结果
.\run_experiment.ps1 -Action analyze -Name "test-1"

# 查看对话
.\run_experiment.ps1 -Action view -Name "test-1"

# 列出所有实验
.\run_experiment.ps1 -Action list
```

**方式二：直接运行Python脚本**

```powershell
# 开始100轮对话
python party_chat.py --name lang-emerge-1 --rounds 100 --novlang-file data\prompts\novlang_rules.txt

# 继续实验
python party_chat.py --name lang-emerge-1 --rounds 100 --resume --novlang-file data\prompts\novlang_rules.txt
```

## 📊 查看结果

### 实时监控

```powershell
# 查看最新对话
Get-Content results\party_chat\lang-emerge-1\rounds.json -Tail 20

# 统计对话轮次
(Get-Content results\party_chat\lang-emerge-1\rounds.json | ConvertFrom-Json).Count
```

### 详细分析

```powershell
# 运行分析工具
python analyze_emergence.py results\party_chat\lang-emerge-1\rounds.json

# 导出时间线数据
python analyze_emergence.py results\party_chat\lang-emerge-1\rounds.json --export-timeline results\party_chat\lang-emerge-1\timeline.json
```

## 🔧 优化对话效率

已经做的优化：

1. **减少人物数量**: 从26人减少到4人
2. **提高思考速度**: interval从1000ms降到500ms
3. **增加对话迭代**: chat_iter从4增加到10
4. **增强记忆保留**: retention从8增加到12
5. **扩大感知范围**: vision_r和att_bandwidth都增加到10

### 进一步优化（可选）

编辑 `data/config.json`:

```json
{
    "agent": {
        "think": {
            "interval": 300,  // 进一步减少到300ms
        },
        "chat_iter": 15,      // 增加到15次迭代
        "associate": {
            "retention": 15   // 增加记忆保留
        }
    }
}
```

## 📁 文件说明

### 主要脚本

- `party_chat.py` - 四人对话实验主脚本
- `start.py` - 传统模拟脚本（已修改为4人）
- `analyze_emergence.py` - 语言涌现分析工具
- `run_experiment.ps1` - PowerShell快捷脚本

### 配置文件

- `data/config.json` - Agent配置（已优化）
- `data/prompts/novlang_rules.txt` - Novlang语言规则
- `data/prompts/generate_chat.txt` - 对话生成提示词

### 输出目录

```
results/party_chat/
  your-experiment-name/
    rounds.json           # 所有对话记录
    conversation.json     # 原始对话数据
    checkpoint-*.json     # 检查点文件
    timeline.json         # 时间线数据（需分析生成）
```

## 🔍 观察语言涌现

### 关键指标

1. **符号多样性**: 使用了多少不同的Novlang符号
2. **组合创新度**: 创造了多少独特的符号组合
3. **稳定组合**: 哪些组合被反复使用（形成共识）
4. **使用集中度**: 是否某些符号被过度使用
5. **演化趋势**: 前期vs后期的符号使用变化

### 分析示例

运行分析后，你会看到：

```
语言涌现分析报告
======================================================================

📊 基础统计
  总轮次: 100
  总对话数: 400

🔤 基础符号使用频率 (Top 10)
  ●  存在/实体       使用次数:  234  占比: 12.50%
  —  线性/过程       使用次数:  189  占比: 10.11%
  ...

🔗 高频符号组合 (Top 15)
  ●·—       出现次数:  45
  △·⌒      出现次数:  32
  ...

✨ 语言涌现指标
  符号多样性: 85.00% (17/20)
  独特组合数: 87
  稳定组合数: 12 (出现≥5次)
  符号集中度: 45.32% (Top5符号占比)
```

## 🛠️ 故障排除

### 问题1: 对话不够频繁

**可能原因**: 
- agents不在同一位置
- 时间间隔太短
- 条件不满足

**解决方案**:
- 检查 `data/config.json` 中的 `interval` 设置
- 增加 `chat_iter` 值
- 查看日志了解跳过对话的原因

### 问题2: 不使用Novlang

**检查**:
1. `--novlang-file` 参数是否正确
2. `data/prompts/generate_chat.txt` 是否包含Novlang规则
3. LLM是否理解符号语言

**解决方案**:
- 在提示词中强调"必须使用Novlang"
- 使用更强大的LLM模型
- 增加示例对话

### 问题3: 程序崩溃

**常见原因**:
- Ollama服务未运行
- embedding模型未加载
- 内存不足

**解决方案**:
```powershell
# 检查Ollama状态
curl http://127.0.0.1:11434/api/tags

# 重启Ollama
ollama serve

# 测试模型
ollama run qwen3:8b-q4_K_M "你好"
```

## 📈 下一步

### 短期目标（1-10轮）
- 观察agents是否开始使用Novlang
- 检查符号使用是否合理
- 调整提示词和参数

### 中期目标（10-100轮）
- 追踪符号组合的稳定化
- 观察共识的形成
- 分析语义收敛

### 长期目标（100+轮）
- 完整的语言涌现过程
- 符号系统的自发演化
- 新表达方式的创造

## 💡 高级技巧

### 1. 自定义对话配对

编辑 `party_chat.py` 中的 `agent_pairs`:

```python
agent_pairs = [
    (self.agents[0], self.agents[1]),  # 只让这两个人对话
]
```

### 2. 添加对话主题

在 `_force_simple_chat` 方法中添加主题提示。

### 3. 导出对话供分析

```python
# 导出为CSV
python export_conversations.py results/party_chat/test-1/rounds.json --format csv
```

### 4. 可视化时间线

使用timeline.json创建图表：
- 符号使用频率随时间变化
- 对话长度趋势
- 组合创新度曲线

## 📝 记录实验笔记

建议为每个实验创建笔记文件：

```powershell
New-Item results\party_chat\test-1\notes.md
```

记录内容：
- 实验假设
- 参数设置
- 观察现象
- 有趣发现
- 改进想法

## 🎓 理论背景

语言涌现是指在多智能体交互中，自发形成共享的通信协议。观察要点：

1. **符号接地** (Symbol Grounding): 符号获得稳定意义
2. **组合性** (Compositionality): 复杂意义由简单符号组合
3. **效率演化** (Efficiency): 交流变得更简洁高效
4. **创新与稳定**: 新表达与共识的平衡

## 📚 参考资料

- Generative Agents论文
- 多智能体通信协议
- 人工语言设计理论
- Novlang语言规范

---

**祝实验顺利！观察语言的涌现是一个激动人心的过程。** 🚀
